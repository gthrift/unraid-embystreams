Menu="Dashboard"
Icon="emby.png"
Tag="film"
---
<?php
    // Initialize session for CSRF protection
    if (session_status() === PHP_SESSION_NONE) {
        session_start();
    }
    
    // Generate CSRF token if not exists
    if (empty($_SESSION['embystreams_csrf'])) {
        $_SESSION['embystreams_csrf'] = bin2hex(random_bytes(32));
    }
    
    $plugin = "embystreams";
    $cfg = parse_ini_file("/boot/config/plugins/embystreams/embystreams.cfg");
    
    // Validate and sanitize poll interval (default 5000ms)
    $poll_interval = isset($cfg['POLL_INTERVAL']) ? max(1, min(60, (int)$cfg['POLL_INTERVAL'])) * 1000 : 5000;
    
    $host = $cfg['HOST'] ?? '';
    $port = $cfg['PORT'] ?? '8096';
    
    // Validate port
    $port = max(1, min(65535, (int)$port));
    
    // Protocol detection
    $use_https = ($cfg['USE_HTTPS'] ?? '0') === '1';
    $protocol = $use_https ? 'https' : 'http';
    
    $emby_url = htmlspecialchars("$protocol://$host:$port/web/index.html", ENT_QUOTES, 'UTF-8');
    $csrf_token = $_SESSION['embystreams_csrf'];
?>

<style>
    /* Namespace all styles to prevent conflicts */
    #embystreams_tile {
        /* Inherit Unraid font settings */
        font-size: 1.2rem;
    }

    /* Column Headers */
    .es-header-row {
        display: flex;
        padding: 4px 0;
        border-bottom: 1px solid var(--border-default); /* Native theme border */
        color: var(--text-muted); /* Native muted text color */
        font-weight: 500;
        font-size: 1.1rem;
        margin-bottom: 8px;
    }

    .es-header-row span {
        flex: 1;
        padding: 0 4px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    
    /* Specific widths for columns to match data content */
    .es-name { flex: 2 !important; }
    .es-device { flex: 1; }
    .es-user { flex: 1; }
    .es-state { flex: 0.8; text-align: right; }

    /* Stream Content Container */
    #embystreams_streams {
        min-height: 40px;
    }

    /* Sub-text styling */
    #es_count_container {
        font-size: 1.1rem;
        color: var(--text-muted);
    }
    
    /* Error State using native red */
    .es-error {
        padding: 15px; 
        text-align: center; 
        color: var(--text-red); /* Native error color */
        font-weight: bold;
    }

    /* Native Unraid Icon overrides */
    .emby-icon-img {
        width: 48px;
        height: 48px;
    }
</style>

<?php
// We wrap the content in a div with a unique ID for styling scope
$mytiles[$plugin]['column1'] = <<<EOT
<tbody class="sortable" title="Emby Streams" id="embystreams_tile">
  <tr>
    <td>
      <div class='tile-header' id='{$plugin}-dashboard-card'>
        <div class='tile-header-left'>
          <img src="/plugins/embystreams/emby.png" class="emby-icon-img icon">
          <div class='tile-text'>
            <span class='tile-header-main' id='{$plugin}-dashboard-title'>Emby Streams</span>
            <span id="es_count_container"><span id="embystreams_count">0</span> Active Stream(s)</span>
          </div>
        </div>
        <div class='tile-header-right'>
          <div class='tile-header-right-controls'>
            <a href="{$emby_url}" target="_blank" title="_(Open Emby WebUI)_">
              <i class="fa fa-fw fa-external-link control"></i>
            </a>
            <a id="{$plugin}-settings-button" href="/Settings/EmbyStreamsSettings" title="_(Settings)_">
              <i class="fa fa-fw fa-cog control"></i>
            </a>
          </div>
        </div>
      </div>
    </td>
  </tr>
  <tr>
    <td>            
        <div class="es-header-row">
            <span class="es-name">_(Name)_</span>
            <span class="es-device">_(Device)_</span>
            <span class="es-user">_(User)_</span>
            <span class="es-state">_(State)_</span>
        </div>
    </td>
  </tr>
  <tr>
    <td>
        <div id="embystreams_streams">
            <div id="es_retrieving">
                <p align="center" style="font-style:italic; text-align:center; padding-top:10px; color:var(--text-muted);">_(Retrieving Data...)_</p>
            </div>
        </div>
    </td>
  </tr>
</tbody>
EOT;

// COMPATIBILITY BLOCK FOR UNRAID 7.2+
// The logic below ensures that if we are NOT on 7.2+, we patch the DOM to look correct.
// On 7.2+, the HTML structure above (using .tile-header) is already native.
$isResponsiveWebgui = version_compare(parse_ini_file('/etc/unraid-version')['version'] ?? "", '7.2', '>=');
if ( ! $isResponsiveWebgui) {
    $mytiles[$plugin]["column1"] .= <<<EOT
        <script>
            $(function() {
                // Legacy support adjustments
                $('#{$plugin}-dashboard-title').css('display', 'block'); 
                $('#{$plugin}-settings-button').prependTo('#{$plugin}-dashboard-card');
            });
        </script>
    EOT;
}
?>

<script>
    var embyPollInterval = <?=$poll_interval;?>;
    var embyCsrfToken = "<?=htmlspecialchars($csrf_token, ENT_QUOTES, 'UTF-8');?>";
    var embyLastUpdate = 0;
    var embyMinInterval = 1000; 
    
    function updateEmbyDashboard() {
        var now = Date.now();
        if (now - embyLastUpdate < embyMinInterval) {
            return;
        }
        embyLastUpdate = now;
        
        $.ajax({
            url: "/plugins/embystreams/embystreams_api.php",
            method: "GET",
            headers: {
                "X-CSRF-Token": embyCsrfToken
            },
            timeout: 5000,
            success: function(data) {
                if (data) {
                    // 1. Update list
                    $("#embystreams_streams").html(data);
                    
                    // 2. Update count
                    var count = $("#embystreams_streams .es-row").length;
                    if(data.indexOf("No active streams") !== -1) { count = 0; }
                    $("#embystreams_count").text(count);
                }
            },
            error: function(xhr, status, error) {
                console.error("Emby Streams Error:", status, error);
                $("#embystreams_streams").html(
                    "<div class='es-error'>_(Connection Error)_</div>"
                );
            }
        });
    }

    $(function() {
        updateEmbyDashboard();
        setInterval(updateEmbyDashboard, embyPollInterval);
    });
</script>
